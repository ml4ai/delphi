      program main
      implicit none

      INTEGER, dimension(10) :: array_one
      INTEGER, dimension(17) :: array_two
      REAL, DIMENSION(3, 5, 10) :: X

      array_one(1) = 10

      end program main
