language: python

python:
  - "3.6"

env:
  global:
    - PIPENV_VENV_IN_PROJECT=1
    - PIPENV_IGNORE_VIRTUALENVS=1
      #
# Using pipenv
install:
  - pip install pipenv
  - pipenv install -d --skip-lock
  - pipenv install --selective-upgrade networkx --skip-lock

script: 
  - export PYTHONPATH=$PYTHONPATH:$TRAVIS_BUILD_DIR
  - make test

