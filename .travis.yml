language: python

python:
  - "3.6"

# Using pipenv
install:
  - pip install pipenv
  - pipenv install -dv --skip-lock
  - pipenv install networkx==2.1 --skip-lock

script: 
  - export PYTHONPATH=$PYTHONPATH:$TRAVIS_BUILD_DIR
  - make test

cache: pip
