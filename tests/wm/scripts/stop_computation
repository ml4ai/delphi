#!/bin/bash

# Author: Joseph Astier

# Check the computation progress of a model or experiment.

printf "Testing Delphi REST API stop computation function:\n"

endpoint="/stop_computation"
login="-u me:ivilab4fun"
url=$(./get_delphi_url)

call="curl $login -s $url$endpoint"

echo $($call)
