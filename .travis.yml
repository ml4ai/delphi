language: python

python:
  - "3.6"

install:
  - export PYTHONPATH=$PYTHONPATH:$TRAVIS_BUILD_DIR
  - pip install -U pipenv
  - pipenv install -d --ignore-pipfile --system

script: pipenv run pytest
cache: pip
